const express = require("express");
const app = express();
const dotenv = require("dotenv");
dotenv.config();


// app.post("/extend", asyn(req,res)=>{
//     const body = await 
// })

app.listen(3000, ()=>{
    console.log("running ..")
})